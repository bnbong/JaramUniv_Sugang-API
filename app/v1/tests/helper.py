# from app.db.database import MariaDBConnection


async def make_fresh_db():
    # async with MariaDBConnection.engine.begin() as conn:
    #     await conn.run_sync(MariaDBConnection.metadata.drop_all)
    #     await conn.run_sync(MariaDBConnection.metadata.create_all)
    pass
